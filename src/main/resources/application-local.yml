spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/freelance_manager
    username: app
    password: password
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
